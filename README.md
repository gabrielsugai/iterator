# Iterator Design Pattern

  O `Design` `Pattern` `Iterator` consiste em criar uma classe que percorra uma estrutura de dados.
  Essa classe pode possuir diversas formas de percorrer um conjunto de dados (do mesmo tipo), nesse repositorio há um exemplo do `Iterator` para `Arrays`.
